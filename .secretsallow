# Known false positives for scripts/secret-scan, by fingerprint.
# Only add a line here after reading the flagged code and confirming there is no
# secret value in it. A fingerprint covers one exact string in one place — if the
# line changes, the entry stops matching and the scanner speaks up again.

# agent-tooling/scripts/wikimedia_enterprise_auth.py:111
#   username, password = _prompt_credentials()
# Tuple assignment from a function call. The generic-secret-assignment rule matched
# "password = " and scored the function name as a high-entropy value. Credentials
# here are prompted at runtime and never written to disk.
3e2e9c2fc02e
